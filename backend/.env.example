# Database configuration for XAMPP MySQL
# Replace with your actual credentials if you created a dedicated user.
# By default, XAMPP uses 'root' with an empty password.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=car_sharing
DB_USER=root
DB_PASSWORD=